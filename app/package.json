{
  "name": "app",
  "version": "1.0.0",
  "main": "dist/server.js",
  "license": "MIT",
  "scripts": {
    "build:app": "yarn tsc -p tsconfig.json",
    "build:release": "rsync -av --include-from='release_build.txt' --exclude='*' ./ ../package",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "start": "node dist/server.js",
    "dev": "nodemon src/server.ts  -p tsconfig.json"
  },
  "dependencies": {
    "body-parser": "^1.20.2",
    "express": "^4.19.2",
    "pug": "^3.0.3"
  },
  "devDependencies": {
    "@tsconfig/node-lts": "^20.1.3",
    "@types/express": "^4.17.21",
    "@types/node": "^20.14.6",
    "globals": "^15.6.0",
    "jest": "^29.7.0",
    "nodemon": "^3.1.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
